# Demo MP4 (Mode 2)

Add your video export here:

```
public/videos/demo.mp4
```

Use the **same** video as your Synthesia embed (export MP4 from Synthesia or your editor) so the existing `en.vtt` / `fr.vtt` timestamps stay in sync.

Without this file, Mode 2 shows an error until the MP4 is added.
